{{#each projectData as |project| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{project.name}}</h2>
    {{!-- <h2>{{{get_emoji}}} {{project.name}}</h2> --}}
  </div>
  <div class="col-md-6">
    <h3>{{project.description}}</h3>
    <div>${{project.needed_funding}}</div>
  </div>
</div>
{{/each}}
